-- // INITIALIZATION \\ --
if not game:IsLoaded() then game.Loaded:Wait() end
local player = game.Players.LocalPlayer
local pgui = player:WaitForChild("PlayerGui")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera

-- // SETTINGS \\ --
local AimSettings = {
	Enabled = false,
	TeamCheck = true,
	WallCheck = true,
	StickyMode = true,
	ESPEnabled = false,
	TargetPart = "Head",
	FOV = 200,
	Smoothing = 0.25,
	MaxDistance = 500, -- Default: 500 studs
	Minimized = false
}

local CurrentTarget = nil
local ESP_Objects = {}

-- // UNIVERSAL PART FINDER \\ --
local function getBodyPart(char)
	if not char then return nil end
	if AimSettings.TargetPart == "Head" then
		return char:FindFirstChild("Head")
	else
		return char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")
	end
end

-- // UI CONSTRUCTION \\ --
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Pro_System_V12"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = pgui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 220, 0, 520)
MainFrame.Position = UDim2.new(0, 20, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

local MinBtn = Instance.new("TextButton")
MinBtn.Size = UDim2.new(0, 30, 0, 30)
MinBtn.Position = UDim2.new(1, -35, 0, 5)
MinBtn.Text = "-"
MinBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
MinBtn.TextColor3 = Color3.new(1, 1, 1)
MinBtn.Parent = MainFrame
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 5)

local ContentFrame = Instance.new("Frame")
ContentFrame.Size = UDim2.new(1, 0, 1, 0)
ContentFrame.BackgroundTransparency = 1
ContentFrame.Parent = MainFrame

local function createToggle(text, pos, callback)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 200, 0, 32)
	btn.Position = pos
	btn.Text = text
	btn.Font = Enum.Font.SourceSansBold
	btn.TextSize = 14
	btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
	btn.TextColor3 = Color3.new(1, 1, 1)
	btn.Parent = ContentFrame
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
	btn.MouseButton1Click:Connect(function() callback(btn) end)
	return btn
end

-- // UI ELEMENTS \\ --
createToggle("Aimbot: OFF", UDim2.new(0, 10, 0, 40), function(btn)
	AimSettings.Enabled = not AimSettings.Enabled
	CurrentTarget = nil
	btn.Text = AimSettings.Enabled and "Aimbot: ON" or "Aimbot: OFF"
	btn.BackgroundColor3 = AimSettings.Enabled and Color3.fromRGB(0, 120, 255) or Color3.fromRGB(35, 35, 35)
end)

createToggle("Sticky Mode: ON", UDim2.new(0, 10, 0, 75), function(btn)
	AimSettings.StickyMode = not AimSettings.StickyMode
	CurrentTarget = nil
	btn.Text = AimSettings.StickyMode and "Sticky Mode: ON" or "Sticky Mode: OFF"
end)

createToggle("ESP: OFF", UDim2.new(0, 10, 0, 110), function(btn)
	AimSettings.ESPEnabled = not AimSettings.ESPEnabled
	btn.Text = AimSettings.ESPEnabled and "ESP: ON" or "ESP: OFF"
	btn.BackgroundColor3 = AimSettings.ESPEnabled and Color3.fromRGB(180, 0, 255) or Color3.fromRGB(35, 35, 35)
end)

createToggle("Team Check: ON", UDim2.new(0, 10, 0, 145), function(btn)
	AimSettings.TeamCheck = not AimSettings.TeamCheck
	btn.Text = AimSettings.TeamCheck and "Team Check: ON" or "Team Check: OFF"
end)

createToggle("Wall Check: ON", UDim2.new(0, 10, 0, 180), function(btn)
	AimSettings.WallCheck = not AimSettings.WallCheck
	btn.Text = AimSettings.WallCheck and "Wall Check: ON" or "Wall Check: OFF"
end)

createToggle("Target: Head", UDim2.new(0, 10, 0, 215), function(btn)
	AimSettings.TargetPart = (AimSettings.TargetPart == "Head") and "Torso" or "Head"
	btn.Text = "Target: " .. AimSettings.TargetPart
end)

local function createInput(text, pos)
	local box = Instance.new("TextBox")
	box.Size = UDim2.new(0, 200, 0, 32)
	box.Position = pos
	box.Text = text
	box.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
	box.TextColor3 = Color3.new(1, 1, 1)
	box.Parent = ContentFrame
	Instance.new("UICorner", box).CornerRadius = UDim.new(0, 6)
	return box
end

local FOVBox = createInput("FOV: " .. AimSettings.FOV, UDim2.new(0, 10, 0, 255))
local SmoothBox = createInput("Smooth: " .. AimSettings.Smoothing, UDim2.new(0, 10, 0, 295))
local DistBox = createInput("Max Dist: " .. AimSettings.MaxDistance, UDim2.new(0, 10, 0, 335))

local FOVCircle = Instance.new("Frame")
FOVCircle.Size = UDim2.new(0, AimSettings.FOV * 2, 0, AimSettings.FOV * 2)
FOVCircle.AnchorPoint = Vector2.new(0.5, 0.5)
FOVCircle.Position = UDim2.new(0.5, 0, 0.5, 0)
FOVCircle.BackgroundTransparency = 0.9
FOVCircle.BackgroundColor3 = Color3.new(1, 1, 1)
FOVCircle.Parent = ScreenGui
Instance.new("UICorner", FOVCircle).CornerRadius = UDim.new(1, 0)

-- // LOGIC FUNCTIONS \\ --

MinBtn.MouseButton1Click:Connect(function()
	AimSettings.Minimized = not AimSettings.Minimized
	ContentFrame.Visible = not AimSettings.Minimized
	MinBtn.Text = AimSettings.Minimized and "+" or "-"
	MainFrame.Size = AimSettings.Minimized and UDim2.new(0, 220, 0, 40) or UDim2.new(0, 220, 0, 520)
end)

-- UPDATE BOX VALUES
FOVBox.FocusLost:Connect(function() 
	local n = tonumber(FOVBox.Text:match("%d+%.?%d*"))
	if n then AimSettings.FOV = n FOVCircle.Size = UDim2.new(0, n*2, 0, n*2) end
	FOVBox.Text = "FOV: " .. AimSettings.FOV
end)

SmoothBox.FocusLost:Connect(function() 
	local n = tonumber(SmoothBox.Text:match("%d+%.?%d*"))
	if n then AimSettings.Smoothing = math.clamp(n, 0.01, 1) end
	SmoothBox.Text = "Smooth: " .. AimSettings.Smoothing
end)

DistBox.FocusLost:Connect(function() 
	local n = tonumber(DistBox.Text:match("%d+%.?%d*"))
	if n then AimSettings.MaxDistance = n end
	DistBox.Text = "Max Dist: " .. AimSettings.MaxDistance
end)

local function isVisible(targetPart)
	if not AimSettings.WallCheck then return true end
	local rayParams = RaycastParams.new()
	rayParams.FilterType = Enum.RaycastFilterType.Exclude
	rayParams.FilterDescendantsInstances = {player.Character, Camera}
	local result = workspace:Raycast(Camera.CFrame.Position, (targetPart.Position - Camera.CFrame.Position), rayParams)
	return result == nil or result.Instance:IsDescendantOf(targetPart.Parent)
end

local function isValid(p)
	if p and p.Parent == Players and p.Character and player.Character then
		local hum = p.Character:FindFirstChild("Humanoid")
		local hrp = p.Character:FindFirstChild("HumanoidRootPart")
		local myHrp = player.Character:FindFirstChild("HumanoidRootPart")
		local targetPart = getBodyPart(p.Character)
		
		if hum and hum.Health > 0 and hrp and myHrp and targetPart then
			-- TEAM CHECK
			if AimSettings.TeamCheck and p.Team and p.Team == player.Team then return false end
			
			-- CRITICAL FIX: ACTUAL DISTANCE CHECK
			local distance = (myHrp.Position - hrp.Position).Magnitude
			if distance > AimSettings.MaxDistance then 
				return false 
			end
			
			-- FOV CHECK
			local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
			if onScreen then
				local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
				if (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude <= AimSettings.FOV then
					return isVisible(targetPart)
				end
			end
		end
	end
	return false
end

local function getNewTarget()
	local closestPlayer = nil
	local shortestDistance = AimSettings.FOV
	local screenCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
	for _, p in pairs(Players:GetPlayers()) do
		if p ~= player and isValid(p) then
			local targetPart = getBodyPart(p.Character)
			local screenPos = Camera:WorldToViewportPoint(targetPart.Position)
			local distToCenter = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
			if distToCenter < shortestDistance then
				shortestDistance = distToCenter
				closestPlayer = p
			end
		end
	end
	return closestPlayer
end

-- // RENDER LOOP \\ --
RunService:BindToRenderStep("FinalSystemV12", Enum.RenderPriority.Camera.Value + 1, function()
	-- 1. CLEANUP GHOST BOXES
	for p, obj in pairs(ESP_Objects) do
		if not p or not p.Parent or not Players:FindFirstChild(p.Name) then
			if obj then obj:Destroy() end
			ESP_Objects[p] = nil
		end
	end

	-- 2. AIMBOT
	if AimSettings.Enabled and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
		if AimSettings.StickyMode then
			if CurrentTarget and not isValid(CurrentTarget) then CurrentTarget = nil end
			if not CurrentTarget then CurrentTarget = getNewTarget() end
		else
			CurrentTarget = getNewTarget()
		end
		
		if CurrentTarget then
			local targetPart = getBodyPart(CurrentTarget.Character)
			if targetPart then
				Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, targetPart.Position), AimSettings.Smoothing)
			end
		end
	end

	-- 3. ESP
	for _, p in pairs(Players:GetPlayers()) do
		local obj = ESP_Objects[p]
		if AimSettings.ESPEnabled and p ~= player and p.Character and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
			if not obj then 
				local box = Instance.new("BoxHandleAdornment")
				box.AlwaysOnTop = true
				box.ZIndex = 10
				box.Transparency = 0.5
				box.Parent = pgui
				ESP_Objects[p] = box
				obj = box
			end
			obj.Adornee = p.Character
			obj.Size = p.Character:GetExtentsSize()
			obj.Color3 = (not p.Team and Color3.new(1,1,1)) or (p.Team == player.Team and Color3.new(0,1,0)) or Color3.new(1,0,0)
			obj.Visible = true
		elseif obj then
			obj.Visible = false
		end
	end
end)
